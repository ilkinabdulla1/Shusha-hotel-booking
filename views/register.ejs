<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Register</title>

  <!-- Bootstrap CSS -->
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <!-- Toastr CSS -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css" rel="stylesheet">

  <style>
    body {
      font-family: "Arial", sans-serif;
      height: 100vh;
      margin: 0;
      background: url('/images/shusha-background.png') no-repeat center center fixed;
      background-size: cover;
      position: relative;
    }

    body::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.6); /* Semi-transparent overlay */
      z-index: 0;
    }

    .register-container {
      display: flex;
      justify-content: center;
      align-items: center;
      width: 90%;
      max-width: 900px; /* Reduced size for better visibility */
      background: rgba(255, 255, 255, 0.95); /* Slight transparency */
      border-radius: 20px;
      padding: 0;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      z-index: 1;
    }

    .info-section,
    .form-section {
      flex: 1;
      padding: 30px; /* Reduced padding for a compact design */
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
    }

    .info-section {
      background: linear-gradient(135deg, #151F54, #0D133D); /* Darker gradient for contrast */
      color: #ffffff;
      border-radius: 20px 0 0 20px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 100px; /* Increased padding for height adjustment */
      height: 100%; /* Ensures it stretches within the container */
    }

    .info-section h1 {
      font-size: 2rem;
      margin-bottom: 15px;
      font-weight: bold;
    }

    .info-section p {
      font-size: 1rem;
      line-height: 1.6;
      margin-bottom: 20px;
    }

    .info-section .circle-logo {
      width: 60px;
      height: 60px;
      border-radius: 50%;
      background: #ffffff;
      display: flex;
      justify-content: center;
      align-items: center;
      margin-bottom: 15px;
    }

    .info-section .circle-logo img {
      width: 40px;
      height: 40px;
    }

    .form-section {
      border-radius: 0 20px 20px 0;
      display: flex;
      flex-direction: column;
      justify-content: center;
      width: 100%; /* Ensure uniform size */
    }

    .form-section h4 {
      font-size: 1.5rem;
      font-weight: bold;
      margin-bottom: 20px;
    }

    .form-section form .form-group {
      margin-bottom: 15px;
      width: 100%;
    }

    .form-section form .form-control {
      padding: 10px;
      border-radius: 5px;
      font-size: 0.95rem;
    }

    .form-section form .btn {
      width: 100%;
      background-color: #0056b3;
      color: #fff;
      padding: 12px;
      border-radius: 5px;
      border: none;
      font-size: 1rem;
      font-weight: bold;
    }

    .form-section form .btn:hover {
      background-color: #00449e;
    }

    .form-section .login-link {
      text-align: center;
      margin-top: 15px;
      font-size: 0.9rem;
    }

    .form-section .login-link a {
      color: #0056b3;
      text-decoration: none;
    }

    .form-section .login-link a:hover {
      text-decoration: underline;
    }

    @media (max-width: 768px) {
      .register-container {
        flex-direction: column;
        max-width: 90%;
      }

      .info-section {
        border-radius: 20px 20px 0 0;
      }

      .form-section {
        border-radius: 0 0 20px 20px;
      }
    }
  </style>
</head>
<body>
  <div class="register-container">
    <!-- Info Section -->
    <div class="info-section">
      <h1>TechTrade Solutions</h1>
      <p>Sign up to explore the features and opportunities we offer.</p>
    </div>

    <!-- Form Section -->
    <div class="form-section">
      <h4>Register</h4>
      <form action="/register" method="POST">
        <!-- Name -->
        <div class="form-group">
          <label for="name">Name</label>
          <input type="text" class="form-control" id="name" name="name" placeholder="Enter your name" required>
        </div>

        <!-- Email -->
        <div class="form-group">
          <label for="email">Email</label>
          <input type="email" class="form-control" id="email" name="email" placeholder="Enter your email" required>
        </div>

        <!-- Password -->
        <div class="form-group">
          <label for="password">Password</label>
          <input type="password" class="form-control" id="password" name="password" placeholder="Create a password" required>
        </div>

        <!-- Submit Button -->
        <button type="submit" class="btn btn-primary">Register</button>
      </form>

      <div class="login-link mt-4">
        Already have an account? <a href="/login">Login</a>
      </div>
    </div>
  </div>

  <!-- jQuery -->
  <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
  <!-- Bootstrap JS -->
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  <!-- Toastr JS -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js"></script>

  <!-- Toastr for Error Message -->
  <script>
    <% if (typeof errorMessage !== 'undefined' && errorMessage) { %>
      toastr.error("<%= errorMessage %>");
    <% } %>
  </script>
</body>
</html>
